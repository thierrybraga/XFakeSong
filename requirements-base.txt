# =====================================================================
# XFakeSong — Dependências COMPARTILHADAS (fonte única)
# =====================================================================
# Tudo EXCETO o TensorFlow. Os arquivos de runtime referenciam este base e
# adicionam apenas a variante de TF:
#   - requirements.txt      → -r requirements-base.txt + tensorflow
#   - requirements-cpu.txt  → -r requirements-base.txt + tensorflow-cpu
# Mantenha as deps compartilhadas SÓ aqui (evita drift entre os arquivos).
# Ambiente de referência: Python 3.11 (compatível 3.11–3.13).
# =====================================================================

# --- ML / DL core ---
keras>=3.14.1,<4.0
scikit-learn>=1.9.0,<2.0
numba>=0.65.1            # 0.60+ cobre Python 3.11–3.13
joblib>=1.5.3

# --- Áudio / DSP ---
librosa>=0.11.0
soundfile>=0.14.0
PyWavelets>=1.9.0

# --- Numéricos ---
numpy>=2.4.6,<2.5       # TF aceita numpy 2.x estável; teto evita majors não testados
# pandas <3: mlflow exige pandas<3 e o pandas 3.0 traz breaking changes
# (evita ResolutionImpossible quando o Dependabot tenta pandas>=3.0.3).
pandas>=3.0.3,<4.0
matplotlib>=3.10.9
seaborn>=0.13.2

# --- Web / API ---
# BUG.Render.1: pin firme gradio>=4.31 — versões anteriores usam a API antiga
# `TemplateResponse(context_dict, name)`; starlette>=0.36 (puxado por fastapi)
# mudou para `(request, name, context)` → `TypeError: unhashable type: 'dict'`
# na 1ª request a "/". fastapi/starlette pinados para reprodutibilidade.
gradio>=6.17.3,<7.0
fastapi>=0.136.3,<0.137
starlette>=1.2.1,<1.3
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32
httpx>=0.28.1
jinja2>=3.1.6
slowapi>=0.1.9
a2wsgi>=1.10.10

# --- Datasets (scripts de download HuggingFace) ---
# Teto <4.0: o datasets 4.x passou a exigir `torchcodec` (torch + FFmpeg) para
# decodificar áudio, quebrando o download (mesmo com Audio(decode=False)). Até
# 3.x o decode é via soundfile — o workaround de `download_datasets.py` funciona.
datasets>=5.0.0,<6.0

# --- Persistência / Config ---
sqlalchemy>=2.0.50
pydantic>=2.13.4
python-dotenv>=1.2.2
pyyaml>=6.0.3
jsonschema>=4.26.0
redis>=8.0.0

# --- Hugging Face / deploy ---
# Teto <1.0: hub 1.0 removeu `HfFolder` que o gradio 4.x importa (quebra a UI no
# boot). Piso 0.25 = exigência do `datasets`; a faixa 0.25–0.36 satisfaz ambos.
huggingface_hub>=1.18.0,<2.0
requests>=2.34.2

# --- Documentação ---
mkdocs>=1.6.1
mkdocs-material>=9.7.6

# --- Testes mínimos (rotas importam pytest em runtime) ---
pytest>=9.0.3

# --- Diagnóstico de GPU ---
# pynvml detecta hardware NVIDIA mesmo sem CUDA no TF (Windows nativo, builds
# CPU). Usado por app.core.gpu no painel de diagnóstico. Opcional (fallback
# nvidia-smi), mas mantido em AMBAS as variantes para paridade.
pynvml>=13.0.1
