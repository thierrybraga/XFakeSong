# =====================================================================
# Ambiente de treino pytorch-audio (familia waveform-end-to-end)
# =====================================================================
# CORREÇÃO 2026-07-28: este arquivo mantinha uma lista PARALELA a
# `requirements-base.txt`, repetindo numpy/pandas/sklearn/librosa/... com
# faixas próprias. As duas já haviam divergido — faltavam aqui `pynvml` (a
# detecção de GPU em `app/core/gpu.py` degradava em silêncio), `python-dotenv`
# e `keras`. Agora herda a base e sobra só o específico deste ambiente.
# 3 niveis ate a raiz: docker/environments/<env>/ -> .. -> ../.. -> ../../..
-r ../../../requirements-base.txt

# As implementações atuais de RawNet2/AASIST/RawGAT-ST/Conformer/Hybrid neste
# repositório são modelos KERAS. O TensorFlow fica aqui até existirem ports
# PyTorch nativos, senão o registry nem importa as arquiteturas. No .nvidia o
# Dockerfile instala `tensorflow[and-cuda]` por cima desta faixa.
tensorflow>=2.16,<2.22

# Diferente das imagens Keras (onde o torch só LÊ state_dict e vem do índice
# CPU), aqui o wheel padrão — CUDA no Linux — é o correto.
torch==2.5.1
torchaudio==2.5.1
