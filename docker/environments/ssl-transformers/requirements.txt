# =====================================================================
# Ambiente de treino ssl-transformers (familia ssl-pretrained)
# =====================================================================
# CORREÇÃO 2026-07-28: mantinha uma lista PARALELA a `requirements-base.txt`,
# com faixas próprias para numpy/pandas/sklearn/librosa/... e sem `pynvml`
# (detecção de GPU degradava em silêncio), `python-dotenv` ou `PyWavelets`.
# Agora herda a base — que já traz `huggingface_hub` e `datasets` — e sobra só
# o específico deste ambiente.
# 3 niveis ate a raiz: docker/environments/<env>/ -> .. -> ../.. -> ../../..
-r ../../../requirements-base.txt

# O harness de benchmark é Keras; TF fica aqui mesmo nesta família. No .nvidia o
# Dockerfile instala `tensorflow[and-cuda]` por cima desta faixa.
tensorflow>=2.16,<2.22

# Esta é a imagem onde o PyTorch TREINA: `run_wavlm_original_benchmark.py`
# roda WavLM Original / HuBERT Original com backbone congelado e cabeça
# treinável. Wheel padrão (CUDA no Linux) — ao contrário das imagens Keras,
# onde o torch só lê state_dict e vem do índice CPU.
torch==2.5.1
torchaudio==2.5.1
transformers==4.45.2
