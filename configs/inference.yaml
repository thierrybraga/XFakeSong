# XFakeSong inference defaults.

inference:
  models_dir: data/models
  benchmark_models_dir: data/models/benchmark_final
  registry_path: data/models/registry.json
  results_dir: data/results
  gradio:
    host: 0.0.0.0
    port: 7860
  cache:
    huggingface: cache/huggingface
    torch: cache/torch
    tensorflow: cache/tensorflow

