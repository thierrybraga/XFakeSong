# =====================================================================
# XFakeSong — Build CPU-only (~250MB menor que padrão)
# =====================================================================
# Usar:
#   pip install -r requirements-cpu.txt
# OU no Dockerfile (override):
#   ARG TF_VARIANT=cpu
#   COPY requirements-${TF_VARIANT:-}.txt requirements.txt
#
# Compatível com tudo do requirements.txt EXCETO substitui tensorflow
# pela variante CPU-only (sem CUDA libs).
# =====================================================================

# Forçar variante CPU.
# BUG.Install: TF 2.16-2.19 não tem wheels Python 3.13; 2.20+ tem. Subido para <2.22.
tensorflow-cpu>=2.21.0,<2.22

# Demais dependências idênticas a requirements.txt
keras>=3.14.1,<4.0
scikit-learn>=1.9.0,<2.0
# numba >=0.65.1 para suporte Python 3.13
numba>=0.65.1
joblib>=1.5.3

librosa>=0.11.0
soundfile>=0.14.0
PyWavelets>=1.9.0

# HuggingFace datasets (scripts de download)
datasets>=5.0.0

# numpy: TF 2.20+ requer >=1.26 e aceita 2.x
numpy>=2.4.6,<2.5
pandas>=3.0.3
matplotlib>=3.10.9
seaborn>=0.13.2

# BUG.Render.1: pin de versão idêntico ao requirements.txt — evita erro
# "unhashable type: dict" causado por incompatibilidade gradio<4.31 +
# starlette>=0.36. Mantenha sincronizado com requirements.txt.
gradio>=6.17.3,<7.0
fastapi>=0.136.3,<0.137
starlette>=1.2.1,<1.3
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32
httpx>=0.28.1
jinja2>=3.1.6
slowapi>=0.1.9
a2wsgi>=1.10.10

sqlalchemy>=2.0.50
pydantic>=2.13.4
python-dotenv>=1.2.2
pyyaml>=6.0.3
jsonschema>=4.26.0
redis>=8.0.0

# Teto <1.0: hub 1.0 removeu `HfFolder` que gradio 4.x importa (quebra a UI).
# Piso 0.25 = exigência do datasets. Ver nota em requirements.txt.
huggingface_hub>=1.18.0,<2.0
requests>=2.34.2

mkdocs>=1.6.1
mkdocs-material>=9.7.6

pytest>=9.0.3
